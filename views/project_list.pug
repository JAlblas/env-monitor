extends layout

block content
  h1= title

  ul
    each project in project_list
      li
        a(href=project.url) #{project.name}
        |  (#{project.description})

    else
      li There are no authors.