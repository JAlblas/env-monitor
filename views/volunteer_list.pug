extends layout

block content
  h1= title

  ul
    each volunteer in volunteer_list
      li
        a(href=volunteer.url) #{volunteer.name}
        |  (#{volunteer.description})

    else
      li There are no volunteers.