extends layout

block content
  h1= title

  ul
    each location in location_list
      li
        a(href=location.url) #{location.location.coordinates}
        |  (#{location.date})

    else
      li There are no locations.